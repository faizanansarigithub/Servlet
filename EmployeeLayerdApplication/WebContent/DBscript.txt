CREATE TABLE "SYSTEM"."EMPLOYEE_LAYERD" 
   (	"ENO" NUMBER(10,0) NOT NULL ENABLE, 
	"ENAME" VARCHAR2(20 BYTE), 
	"EADD" VARCHAR2(20 BYTE), 
	"DEPT" VARCHAR2(20 BYTE), 
	"SALARY" FLOAT(10), 
	 CONSTRAINT "EMPLOYEE_LAYERD_PK" PRIMARY KEY ("ENO")
	 
	 
   CREATE SEQUENCE  "SYSTEM"."EMP_SEQ"  MINVALUE 1 MAXVALUE 600000 INCREMENT BY 1 START WITH 1 ;